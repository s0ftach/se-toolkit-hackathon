# Copy this to .env and fill in your values
# Leave blank to use defaults (port 80, auto-generated secret)
JWT_SECRET=dev-secret-change-me
# HOST_PORT=80  # Change this to use a different port (e.g. 8080)
